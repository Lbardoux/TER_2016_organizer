debut1, fin1, debut2, fin2 = 0, 0, 0, 0
		compteur = 0
		while compteur < 150: #on impose une borne au cas ou
			try:
				debut1, fin1 = avoirPremierCreneauDepuis(creneaux1, compteur)
				debut2, fin2 = avoirPremierCreneauDepuis(creneaux2, compteur)
			except IndexError:
				break #sort du while, car c'est fini :D
			else:
				# définition de l'intervalle a capturer
				departIteration, finIteration = definirBornes(debut1, fin1, debut2, fin2)
				liste1 = capturer(creneaux1, departIteration, finIteration)
				liste2 = capturer(creneaux2, departIteration, finIteration)
				#lance la comparaison sur cet intervallle
				
				compteur = finIteration
			#try
		#while
